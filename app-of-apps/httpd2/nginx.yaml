apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment2
  annotations:
    argocd.argoproj.io/sync-wave: "100" 
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 5
  template:
    metadata:
      labels:
        app: nginx
    spec:
      initContainers:
      - name: init-myservice
        image: busybox:1.28
        command: ['sh', '-c', "sleep 120"]
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
